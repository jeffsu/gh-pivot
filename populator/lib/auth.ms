var request = require('request');
export class Auth {
  function initialize(options) {
    this.username = options.username;
    this.password = options.password;
  }

  function get(repo, url, params, cb) {
     
  }

  function post(repo, url, parmas, cb) {

  }
}
